import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 51,
    height: 31,
    borderRadius: 31,
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 3,
    justifyContent: 'flex-end',
  },
  circle: {
    width: 27,
    height: 27,
    borderRadius: 27,
    backgroundColor: '#fff',
  },
});
